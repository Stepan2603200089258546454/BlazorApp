@using BlazorApp.Client.Models.Cloud

<div class="modal fade" id="@(NameModal)" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="@(NameModal)Label" aria-hidden="true">
    <div class="modal-dialog modal-fullscreen modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="@(NameModal)Label">Окно информации</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Закрыть"></button>
            </div>
            @if (Model != null)
            {
                <div class="modal-body">
                    <p>Отображаемое имя: @Model.DisplayName</p>
                    <p>Системное имя: @Model.SystemName</p>
                    <p>Описание: @Model.Description</p>
                    @if (Model.Size.HasValue)
                    {
                        <p>Размер: @FileExstensionHelper.FormatBytes(Model.Size.Value)</p>
                    }
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
                </div>
            }
            else
            {
                <div class="modal-body">
                    Не выбран элемент
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
                </div>
            }
        </div>
    </div>
</div>

@code {
    [Parameter, EditorRequired]
    public CloudItemModel? Model { get; set; }
    [Parameter, EditorRequired]
    public string NameModal { get; set; }
}
